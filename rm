#include <stdio.h>
#include <unistd.h>

int main(int argc, char *argv[])
{
    if (unlink(argv[1]) == -1) {       // glibc unlink() → unlink() syscall
        perror("unlink");
        return 1;
    }

    return 0;
}
